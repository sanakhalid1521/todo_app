Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

{{- if .Values.frontend.ingress.enabled }}
APPLICATION ACCESS:
------------------
You can access your application at:
{{- range $host := .Values.frontend.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.frontend.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
APPLICATION ACCESS:
------------------
Get the application URL by running these commands:
  echo "Frontend Service: kubectl --namespace {{ .Release.Namespace }} get svc {{ include "todo-app.frontend.fullname" . }}"
  echo "Backend Service: kubectl --namespace {{ .Release.Namespace }} get svc {{ include "todo-app.backend.fullname" . }}"
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "todo-app.frontend.fullname" . }} 3000:3000 &
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "todo-app.backend.fullname" . }} 8000:8000 &
{{- end }}

CONFIGURATION:
--------------
To configure your application, you will need to provide the following secrets:
- DATABASE_URL: Your database connection string
- OPENAI_API_KEY: Your OpenAI API key
- BETTER_AUTH_SECRET: Your authentication secret

You can update these values by running:
  $ helm upgrade {{ .Release.Name }} --set secrets.databaseUrl="YOUR_ENCODED_DB_URL" --set secrets.openaiApiKey="YOUR_ENCODED_OPENAI_KEY" --set secrets.authSecret="YOUR_ENCODED_AUTH_SECRET"